export enum AppRoute {
  Catalog = '/',
  CatalogPage = '/page-:id',
  Product = '/catalog/:id',
  Basket = '/basket',
}

export enum APIRoute {
  Cameras = '/cameras',
  Promo = '/promo',
}

export const PAGE_SIZE = 9;
